import {requireAdmin,sameOrigin,HttpError,fail} from '@/lib/server';
export async function POST(request:Request){try{sameOrigin(request);await requireAdmin();if(Number(request.headers.get('content-length')||0)>4500000)throw new HttpError(413,'Envie uma imagem de até 4 MB.');const form=await request.formData();const file=form.get('image');if(!(file instanceof File)||file.size>4000000||file.type!=='image/webp')throw new HttpError(400,'Selecione uma imagem WebP de até 4 MB.');const bytes=new Uint8Array(await file.arrayBuffer());if(String.fromCharCode(...bytes.slice(0,4))!=='RIFF'||String.fromCharCode(...bytes.slice(8,12))!=='WEBP')throw new HttpError(400,'Arquivo WebP inválido.');const filename=`${crypto.randomUUID()}.webp`;const r=await fetch(`${process.env.SUPABASE_URL}/storage/v1/object/product-images/${filename}`,{method:'POST',headers:{apikey:process.env.SUPABASE_SERVICE_ROLE_KEY!,Authorization:`Bearer ${process.env.SUPABASE_SERVICE_ROLE_KEY}`,'Content-Type':'image/webp','x-upsert':'false'},body:bytes,signal:AbortSignal.timeout(20000)});if(!r.ok)throw new HttpError(503,'Não foi possível enviar a foto. Tente novamente.');return Response.json({url:`${process.env.SUPABASE_URL}/storage/v1/object/public/product-images/${filename}`})}catch(e){return fail(e)}}
